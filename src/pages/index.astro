---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
---

<!doctype html>
<html lang="fr">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body> 
		<Header />
		<main class="home">
			<div class="centered-container">
				<div class="image-container">
					<img src="../img/me.jpg" alt="Photo de moi-même" />
				</div>
				<div class="text-container">
					<h1 id="hacker-text">Bonjour, <br />Je suis Maxime Violette !</h1>
					<p>
						Passionné par les nouvelles technologies, j’ai choisi de me 
						spécialiser en ingénierie des réseaux informatiques et des objets connectés après 
						avoir obtenu mon bac. J’ai intégré <b>UniLaSalle Amiens</b>, une école d’ingénieurs 
						qui m’a permis, dès la troisième année, de suivre ma formation en alternance. 
						C’est dans ce cadre que j’ai rejoint Valeo en tant que <b>R&D DSIS Operation Engineer</b>,
						poste que j'occupe actuellement jusqu'en aout 2026.
						<br/>
						Pour en savoir un peu plus sur moi ou sur mes projets, tu peux consulter
						la section <a href="/blog">Projets</a> <!-- et la page <a href="/about">À propos</a> -->.
						<br/>
						N’hésite pas à consulter mon
						<a href="/cv.pdf" target="_blank" rel="noopener noreferrer">CV</a>,
						ou à <a href="mailto:maxime.v60@orange.fr&body=Sur quoi puis-je t'aider ?">me contacter</a>
						pour discuter ou collaborer !
					</p>
				</div>
			</div>
		</main>
		<Footer />
	</body>
</html>
<script>
	import { gsap } from "gsap";
	import { ScrollTrigger } from "gsap/ScrollTrigger";

	gsap.registerPlugin(ScrollTrigger);

	const runHeroAnimations = () => {
		const tl = gsap.timeline({ defaults: { ease: "power2.out", duration: 0.9 } });

		tl.from(".image-container img", { y: 40, opacity: 0, scale: 1 })
			.from("#hacker-text", { y: 30, opacity: 0 }, "-=0.4")
			.from(".text-container p", { y: 20, opacity: 0 }, "-=0.3")
			.from(".text-container a", { y: 20, opacity: 0 }, "-=0.3");

		gsap.from(".image-container img", {
			scrollTrigger: {
				trigger: ".image-container",
				start: "top 80%",
			},
			scale: 0.98,
			duration: 0.6,
			ease: "power1.out",
		});
	};

	document.addEventListener("DOMContentLoaded", runHeroAnimations);
	document.addEventListener("astro:page-load", runHeroAnimations);
</script>
<script>
  const userLang = navigator.language;
  if (!userLang.startsWith('fr')) {
    window.location.href = '/en/';
  }
</script>
